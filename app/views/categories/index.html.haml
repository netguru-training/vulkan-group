%h1.text-center All categories
%table.table.table-striped
  %tr
    %th
    %th.pull-right Number of products
  - categories.each do |category|
    %tr
      %td= link_to category.name, category
      %td.pull-right= category.products.count
