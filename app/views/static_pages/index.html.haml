= render 'layouts/carousel'

.row
  - discounted_products.each do |product|
    .col-md-4
      = link_to category_product_path(product.category, product) do
        .thumbnail= image_tag product.image
